/*Defines and exports a constant named API_URL that stores the base URL of the backend API (http://localhost:3000/api/v1). It is typically used throughout the application to centralize the API endpoint configuration, making it easier to build request URLs consistently and update the base path in one place if the backend environment changes (e.g., from development to production). */

export const API_URL = "http://localhost:3000/api/v1";
